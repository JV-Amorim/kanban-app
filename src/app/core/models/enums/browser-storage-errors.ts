export enum BrowserStorageErrors {
  GetError = 'Error when inserting the corresponding data.',
  InsertionError = 'Error when inserting new data.',
  DeletionError = 'Error when deleting the corresponding data.'
}
